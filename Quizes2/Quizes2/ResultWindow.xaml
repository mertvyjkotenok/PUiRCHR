<Window x:Class="Quizes2.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:Quizes2.Controls"
        mc:Ignorable="d"
        Title="Результат" Width="707" Height="514" >

    <Grid Margin="0,0,0,-6" Background="Transparent">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Текст -->
        <StackPanel Margin="20" IsHitTestVisible="False">

            <TextBlock x:Name="TitleText"
                       FontSize="24" FontWeight="Bold"
                       TextWrapping="Wrap" Margin="0 0 0 10"/>

            <TextBlock x:Name="ScoreText"
                       FontSize="18"
                       TextWrapping="Wrap" Margin="0 0 0 10"/>

            <TextBlock x:Name="ResultText"
                       FontSize="18"
                       TextWrapping="Wrap"/>

        </StackPanel>
        <ctrl:Cube3DControl x:Name="Cube3"
                            Grid.Column="1" Margin="12,44,12,20" Background="Transparent"/>

        <!-- КУБ -->

    </Grid>
</Window>
